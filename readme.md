# 🏦 Loan Predictor App

A simple **Streamlit-based Machine Learning web app** that predicts whether a loan application will be **approved or denied** based on applicant details.

## 🚀 Features
- Interactive user inputs via sidebar  
- Uses pre-trained model, scalers, and encoders  
- Displays real-time prediction results  
- Styled UI with custom images and headers  

## ⚙️ Requirements
Install dependencies:
```bash
pip install streamlit pandas joblib scikit-learn xgboost matplotlib seaborn
```

## ▶️ How to Run
1. Save all `.pkl`, `.csv`, and `.png` files in the same folder as `loan_prediction.py`
2. Run the app:
```bash
streamlit run loan_prediction.py
```
3. Open the local URL (e.g., http://localhost:8501) in your browser.

## 🧠 About
Developed by **Team Salmon Winners**  
Built with ❤️ using **Python** and **Streamlit**
